# =============================================
# Plastic Waste Audit & Reduction Tracker
# Application Configuration
# =============================================

# Server
server.port=8089
server.servlet.context-path=/

# MySQL Database Configuration (Commented out for H2)
# spring.datasource.url=jdbc:mysql://localhost:3306/plastic_waste_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=root
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:plastic_waste_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# HikariCP Connection Pool
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10

# JPA / Hibernate 6.x (CO3)
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.open-in-view=false

# Initialize Schema
spring.sql.init.mode=never
spring.sql.init.continue-on-error=true

# Thymeleaf
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Logging
logging.level.com.plasticaudit=DEBUG
logging.level.org.springframework.security=INFO
logging.file.name=logs/plastic-waste-tracker.log

# Async Thread Pool (Multithreading - CO1)
spring.task.execution.pool.core-size=4
spring.task.execution.pool.max-size=8
spring.task.execution.pool.queue-capacity=100
spring.task.execution.thread-name-prefix=ReportAggregator-

# Alert Socket Server Port (CO4)
alert.socket.port=9090
alert.socket.enabled=true

# Application Name
spring.application.name=plastic-waste-tracker
